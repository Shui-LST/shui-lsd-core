const { Conflux } = require('js-conflux-sdk');
const ethers = require('ethers');

/*
# Monitor

1. pos node is running
2. sync script is running
3. sCFX price in swappi is bigger than 1
4. how to monitor contract asset is not stolen?
*/

async function main () {

}

main().catch(err => {
  console.error(err);
  process.exit();
});
